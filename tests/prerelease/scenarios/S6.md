# S6 — delegated worktree execution  [KNOWN-UNCERTAIN]

Axes: state = greenfield; mode = solo; execution = **delegated worktree**.

## STATUS: known-flaky / uncertain

On 2026-07-15 the nested arrangement (this subagent spawning a further
`isolation: "worktree"` sub-subagent) silently collapsed onto the primary
checkout — commits on `main`, `worktree-align.sh` printing `aligned=true`
throughout. Root cause unresolved: possibly a harness limit on two-deep
worktree nesting, not an ArDD defect. A fix adding a positive
"am I actually in a separate worktree" check to `worktree-align.sh` was
tasked; this scenario now primarily verifies that check makes the failure
LOUD. A collapse result here is a harness-ambiguity note, not
automatically an ArDD bug. The real answer needs a dedicated top-level
interactive investigation (see README coverage backlog). Ideally the
dispatcher runs THIS scenario from the top-level session (one nesting
level), not as a background subagent (two levels).

## Setup

- `git init $SCRATCH/s6/app` (no remote). Install ArDD from local source.
- `/ardd-init`: trivial concept (e.g. temperature converter CLI), solo,
  `next_step_prompt: false`. `/ardd-backlog` one feature; `/ardd-plan`,
  approve.

## Steps

1. `/ardd-implement`; at the delegation gate choose **delegate**. Follow
   the skill: `inflight-worktrees.sh` pre-check, spawn the
   `isolation: "worktree"` subagent whose mandatory first act is
   `worktree-align.sh`.
2. FIRST verification, before anything else: did a real second worktree
   exist? `git worktree list` from the primary; compare the subagent's
   reported branch/path. Capture align output verbatim. If collapsed:
   record everything, mark the run `collapse-observed`, note whether the
   positive check fired loudly, and stop the delegation portion.
3. If real: let the run complete; coordinator path — `core.bare` check,
   eager merge, `worktree-reap.sh` (surface any `reaped=false` verbatim).
4. Verify post-merge: code + state landed together in one merge; reap
   removed the worktree and `git branch -d` succeeded; `/ardd-status`
   In Flight is empty.

## Scripted answers

- delegation gate: delegate/background.
- eager-merge offer: yes.
- Any next-step prompt: decline.

## Checklist

- [ ] real second worktree confirmed (or collapse loudly detected)
- [ ] align/fold/reap outputs captured verbatim
- [ ] merge landed code + state atomically
- [ ] reap cleaned up; in-flight empty after
