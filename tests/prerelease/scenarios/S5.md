# S5 — solo inline core loop (the plain happy path)

Axes: state = greenfield; mode = solo; execution = inline. Smoke-tier.

## Purpose

The path most users live on: fresh project, backlog→plan→implement
inline, producing real, running code. If this scenario is rough, the
release is rough.

## Setup

- `git init $SCRATCH/s5/app` (no remote, ever). Install ArDD from the
  local source checkout.
- `/ardd-init`: small-but-real concept — e.g. a `wc`-style line/word
  counter CLI with a `--json` output mode. `workflow_mode: solo`,
  `next_step_prompt: true` (deliberately, to exercise the prompt path in
  status/plan — treat each prompt per Scripted answers).

## Steps

1. `/ardd-backlog` two features (core counting; `--json` mode).
2. Run `/ardd-plan` bare (no arguments): with two `backlogged` features
   present it must open the multi-select plannable-inputs picker listing
   both backlogged slugs (plus any open feedback); no picker, or a run
   that silently plans everything, is a bug. Select ONE feature (core
   counting) and confirm the run proceeds scoped to it — the drafted
   plan's scope must name only the selected feature.
3. Continuing the same plan run: solo mode must show NO branch gate — plan+tasks commit
   straight to the current branch (a gate appearing is a bug). Approve at
   the checkpoint. Judge task phrasing: do tasks assume code that doesn't
   exist yet? (2026-07-15 finding class — re-check.)
4. `/ardd-implement` inline. At the delegation gate choose inline. Build
   and RUN the tool; paste real invocation output into your report.
5. Verify state flips: tasks file ready→in-progress→completed,
   checkboxes, register tasked→implemented for the planned feature (the
   unselected `--json` feature must still read `backlogged`), all on the
   branch you're on.
6. `/ardd-status`: counts and In Flight section must match disk; with
   `next_step_prompt: true` it should end with exactly one concrete
   next-step offer (more than one prompt per turn end is a bug).

## Scripted answers

- init: as in Setup.
- bare-plan picker: select the core-counting feature only.
- plan approval: approve unchanged.
- delegation gate: inline.
- every next-step prompt: decline (but record what was offered and
  whether it was a concrete runnable `/ardd-*` invocation).

## Checklist

- [ ] bare `/ardd-plan` presented the picker; run scoped to the selection
- [ ] no branch gate in solo plan
- [ ] task phrasing correct for a from-scratch build
- [ ] code builds and runs; output pasted
- [ ] all three state flips landed correctly
- [ ] status accurate; exactly one next-step prompt, concrete
