# S4 — collaborative-mode lifecycle (local bare origin)

Axes: state = greenfield; mode = **collaborative**; execution = inline;
push path = LOCAL BARE REPO only.

## Purpose

Full backlog→plan→implement loop under `workflow_mode: collaborative`:
the branch gate, the never-commit-to-local-default rule, and the
push/draft-PR offer — exercised safely against a bare repo, never GitHub.

## Setup

- `git init --bare $SCRATCH/s4/origin.git`
- `git init $SCRATCH/s4/app`; add the bare repo as `origin`; initial
  commit; push main. Install ArDD from the local source checkout.
- `/ardd-init`: trivial concept (e.g. a stopwatch CLI),
  `workflow_mode: collaborative`, `next_step_prompt: false`.

## Steps

1. `/ardd-backlog` one small feature (e.g. "lap times").
2. `/ardd-plan`: expect the collaborative branch gate (regular branch,
   never a worktree). Accept the suggested branch name. Approve the plan
   at the checkpoint; let tasking run.
3. `/ardd-implement` inline on that branch, building real working code.
4. At the push/draft-PR offer: confirm the push (to the bare origin);
   `gh pr create` MUST NOT be attempted for real — a bare local origin
   has no GitHub, so judge how the skill handles that failure path (this
   was a 2026-07-15 finding; verify the documented behavior now exists).
5. Verify no commit ever landed on local `main` directly; the register
   flip rides the branch; merge the branch into main locally (simulating
   PR merge), rerun `/ardd-status`, confirm the completion-flip check
   catches/settles state correctly.

## Scripted answers

- init: as in Setup.
- branch gate: accept suggestion.
- plan approval: approve unchanged.
- push offer: yes, push (bare origin only).
- draft-PR offer: yes — expecting graceful `gh` failure handling, and
  remember guardrail 5: if it would hit real GitHub, abort and report.
- Any next-step prompt: decline.

## Checklist

- [ ] branch gate appeared and offered only a regular branch
- [ ] zero direct commits to local main before merge
- [ ] push to bare origin worked with confirmation
- [ ] `gh pr create` failure path handled per docs
- [ ] register flip landed with the merge; status coherent after
- [ ] built code actually runs
