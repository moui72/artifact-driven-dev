---
plan: plan-35f6-2026-07-22-93b3.md
generated: 2026-07-22
status: in-progress
---

# Tasks

## Phase 1: Red-first regression test
- [x] T001 Create `scripts/test-install-skill-sigil.sh` (new file,
      mirroring `scripts/test-install-harness.sh`'s structure: `set -e`,
      the `GIT_DIR`-unset guard, a `mktemp -d` work dir with a cleanup
      trap, `ok`/`bad` helpers). Install a fixture target with
      `--harness claude` and a second with `--harness codex` from this
      same source tree. Assert against the **codex** install's
      `.agents/skills/*/SKILL.md` copies:
      - `ardd-plan/SKILL.md`'s own `# /ardd-plan` H1 heading became
        `# $ardd-plan`.
      - A body cross-reference to another skill (e.g. the literal text
        `` `/ardd-backlog` `` inside `ardd-plan/SKILL.md`) became
        `` `$ardd-backlog` ``.
      - `ardd-update/SKILL.md`'s path reference to
        `.claude/skills/ardd-scripts/ardd-state.sh` was rewritten by the
        *existing* pass to `.agents/skills/ardd-scripts/ardd-state.sh`
        and stays exactly that — `ardd-state.sh` is untouched, never
        becomes `$ardd-state.sh` or has its `/ardd-state` fragment
        corrupted.
      - Wherever `ardd-update/SKILL.md` references
        `ardd-update-check.sh`/`ardd-update-check`, it stays untouched —
        confirms the trailing-boundary check stops `/ardd-update` from
        over-matching into `/ardd-update-check`.
      Also assert the **claude** install's `.claude/skills/*/SKILL.md`
      copies are byte-identical to a plain `git diff`-free baseline (i.e.
      identical to what today's `install_skill_file()` produces) across
      every installed `SKILL.md`, not just a sample — confirming the
      codex-only branch leaks no behavior into the claude path.
- [x] T002 Run `scripts/test-install-skill-sigil.sh` and confirm it
      fails red (the rewrite doesn't exist in `install.sh` yet). Do not
      proceed to Phase 2 until this is confirmed failing for the right
      reason (missing rewrite, not a test-harness bug).

## Phase 2: Implement the rewrite
- [x] T003 In `install.sh`'s `install_skill_file()`, add a codex-only
      `sed` pass — applied to the output of the existing
      `.claude/skills` → `$SKILLS_REL` substitution, before it's written
      to `dest` — that rewrites `/ardd-<name>` to `$ardd-<name>` only
      when: (a) immediately preceded by a command-context boundary
      (backtick, space, `(`, `"`, or start-of-line); (b) `<name>` is
      exactly one of the 14 real skill names (explicit alternation:
      audit, backlog, defects, diagram, feedback, implement, init, lint,
      plan, refine, research, status, tracker, update — never a
      wildcard); (c) immediately followed by a non-identifier character
      (not a lowercase letter or hyphen), so `/ardd-update` cannot match
      inside `/ardd-update-check`. Apply this pass only on the codex
      branch of the harness `case` — the claude branch's output must
      stay byte-identical to today. Update the function's header comment
      (which currently states invocation syntax is "never rewritten") to
      describe the new codex-specific behavior precisely.
- [ ] T004 Run `scripts/test-install-skill-sigil.sh` and confirm it
      passes green. Then run the full regression suite (every
      `scripts/test-*.sh`) and confirm nothing else regressed.
