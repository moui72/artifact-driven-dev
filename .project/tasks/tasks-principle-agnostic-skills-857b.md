---
plan: plan-principle-agnostic-skills-2026-07-10.md   # exact filename of the source plan — authoritative binding
generated: 2026-07-10
status: completed   # generating -> ready -> in-progress -> completed (schema-of-record: scripts/lint-project.sh)
---

# Tasks

## Phase 1: Make ardd-plan principle-agnostic

Edit target is the product source `skills/ardd-plan/SKILL.md` (NOT the
gitignored `.claude/skills/ardd-plan/` install copy — that is regenerated by
`install.sh`). The guiding precedent for all three tasks is `/ardd-analyze`
step 3, which already conditions correctly: "Flag shortcuts that lack a
production annotation entry (**if the constitution includes a production
annotation principle**)." Mirror that "read the declared principles; act only
on what's present" shape. Scope guards: do NOT touch `/ardd-critique`'s
lenses, and do NOT touch any "constitution Principle II" meta-reference
(those name ARDD's own constitution, not a target's).

- [x] T001 [artifacts: constitution] In `skills/ardd-plan/SKILL.md` step 6
  ("Check constitution compliance"), rewrite so it reads the principles the
  target `constitution.md` actually declares — not a fixed list — and flags
  planned patterns that violate or need justification under any *declared*
  principle. Make the Complexity-Tracking flag conditional: only flag
  patterns needing a Complexity Tracking entry when the constitution declares
  a simplicity / complexity-justification principle; when it declares no such
  principle, there is nothing to flag at that site. Do not name "the
  simplicity principle" as an unconditional given.

- [x] T002 [artifacts: constitution] In `skills/ardd-plan/SKILL.md` step 8,
  rewrite the **Complexity Tracking** deliverable so it is included only when
  the constitution declares a principle requiring complexity/deviations to be
  justified (e.g. a Simplicity/YAGNI principle); the section is omitted
  entirely when no such principle exists. Keep the wording symmetric with the
  step-6 phrasing from T001. [parallel]

- [x] T003 [artifacts: constitution] In `skills/ardd-plan/SKILL.md` step 8,
  rewrite the **Production Annotation Summary** deliverable so it is included
  only when the constitution declares a production-annotations principle,
  mirroring `/ardd-analyze` step 3's exact condition; omit the section
  otherwise. [parallel]

## Verification

- [x] T004 Confirm the demonstrable increment by reasoning through the edited
  prose: a plan drafted against a constitution declaring *neither* a
  simplicity nor a production-annotations principle would produce no
  Complexity Tracking and no Production Annotation Summary section; one
  declaring both would produce both — with no other `/ardd-plan` behavior
  changed. Then run `.claude/skills/ardd-scripts/lint-docs.sh` to confirm no
  skill-reference regressions (this is a prose change to a skill, so no
  frontmatter-schema or `lint-project.sh` change is expected). Note there is
  no automated test for skill *behavior* by design (CLAUDE.md) — this task is
  a read-through, not a code test.
