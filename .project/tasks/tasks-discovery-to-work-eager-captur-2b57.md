---
plan: plan-discovery-to-work-eager-captur-2026-07-15-156b.md
generated: 2026-07-15
status: ready   # generating -> ready -> in-progress -> completed (schema-of-record: scripts/lint-project.sh)
---

# Tasks

## Phase 1: Eager capture (discovery-to-work-eager-captur)

- [ ] T001 Add a terminal "capture newly documented capabilities" step to `skills/ardd-init/SKILL.md`, on both the greenfield and existing-codebase paths, running after the artifacts are written: the agent enumerates capabilities the just-written artifacts describe that have (a) no entry in `.project/features/` (any status, including implemented/retired) and (b) no existing implementation, then presents them in ONE batched AskUserQuestion (multiSelect, per-item accept/decline — mirror `/ardd-feedback`'s re-file prompt pattern, never N sequential prompts). For each accepted item: derive a slug via `ardd-state.sh slug` and create the entry via `printf '%s\n' "<description>" | ardd-state.sh feature-create <slug>`. Declined items are simply not created — the user's judgment is final for the run. Keep this distinct from the existing code-based feature-register bulk-extraction step (that reads code; this reads artifacts) unless unifying them reads more clearly in place — plan's open question, decide while editing. Use explicit actor language per constitution Principle IX. No test task: documentation/prose (Principle V exception). Addresses F001 (discovery limbo).
- [ ] T002 [parallel] Add the same terminal capture step to `skills/ardd-refine/SKILL.md`, scoped to the DELTA of this refine: only capabilities newly introduced or materially changed by the edit just applied are candidates (the pivot case — the user must not be re-prompted about long-standing documented scope on every refine). Same batched-confirmation shape, same `feature-create` path, same dedupe-against-all-register-statuses rule as T001. Applies on both the update path and the create-from-template path (where the whole new artifact is the delta). Principle IX actor language; no test task (Principle V exception). Addresses F002 (pivot limbo).

## Phase 2: Retroactive sweep (backlog-sweep-reconcile-from-a)

- [ ] T003 [parallel] Add an advisory "Documented but untracked" section to `skills/ardd-status/SKILL.md`: during discovery, the agent compares stable artifacts' described capabilities against the feature register (all statuses) and existing implementation, and lists unmatched capabilities in both the report and STATUS.md with a pointer to `/ardd-backlog --from-artifacts`. Detection/visibility ONLY — this section must state explicitly that `/ardd-status` never writes the register here (preserving the single-writer list in CLAUDE.md; the orphaned-flip confirmation remains the sole exception). Skip `status: draft` artifacts (draft scope isn't settled enough to nag about). Omit the section when nothing is untracked. Principle IX actor language; no test task (Principle V exception). Addresses F001/F003.
- [ ] T004 [parallel] Add a `--from-artifacts` mode to `skills/ardd-backlog/SKILL.md` (document in its Usage section): instead of logging one described idea, the agent walks every `status: stable` artifact, proposes candidate register entries grounded in specific artifact passages (capability-level noun phrases, deduplicated against the existing register including implemented/retired slugs), presents them in ONE batched multiSelect confirmation, and creates each approved entry via the existing step-2/3 slug + `feature-create` path (project-lock check/touch as in the normal mode). Emphasize the proposal-list framing: capability-vs-design-note classification is agent judgment, the human decides; declined candidates are dropped, not recorded. Note the existing step-6 `/ardd-status` handoff covers the refresh. Principle IX actor language; no test task (Principle V exception). Addresses F001/F003.
- [ ] T005 [parallel] Add a short routing note to `skills/ardd-defects/SKILL.md`: documented-but-never-built scope (a capability an artifact describes that was never implemented at all) is backlog territory, not drift — the agent should route it to `/ardd-backlog --from-artifacts` rather than recording it as a DEFECTS.md entry, reserving DEFECTS.md for genuine code-vs-artifact divergence in built behavior. Principle IX actor language; no test task (Principle V exception). Addresses F003.

## Phase 3: Docs (depends on Phases 1–2)

- [ ] T006 Update the hand-written bodies (below the `generated:end` marker) of `docs/reference/skills/ardd-init.md`, `ardd-refine.md`, `ardd-status.md`, `ardd-backlog.md`, and `ardd-defects.md` to document the new capture step, delta-scoped capture, "Documented but untracked" advisory section, `--from-artifacts` mode, and defects routing note respectively; touch `USAGE.md` and `docs/guides/` only where the affected flows are actually described. If any skill frontmatter `description:` changed in T001–T005, regenerate the affected reference-page headers the way the repo's convention requires. Run `./scripts/lint-docs.sh` and fix any failures before finishing. No test task beyond that lint: documentation (Principle V exception).
