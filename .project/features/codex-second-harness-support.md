---
slug: codex-second-harness-support
status: backlogged
logged: 2026-07-15
---

Single-source Codex CLI support: `install.sh --harness codex` installs the canonical skill set to `.agents/skills/`, preserving ArDD's `.project/` workflow and deterministic scripts. Harness-specific behavior uses explicit, tested clauses and mechanical path/sigil rewrites where safe — never a parallel Codex prose tree or a premature general adapter system. Inline implementation is the safe fallback until a live capability matrix proves Codex can meet ArDD's delegation/worktree/state-landing contract.

Why: the July research established explicit `$` invocation and the shared skill format; the July 20 draft plan refresh adds current Codex evidence for `.worktreeinclude`, stable hooks, and subagent workflows. Automatic skill-to-skill chaining is tested as an enhancement, not a no-go: an explicit next `$ardd-*` invocation remains a functional fallback. The existing constitution currently overstates the unimplemented port as shipped; the feature's implementation plan includes a governed correction when the supported surface is finalized.
