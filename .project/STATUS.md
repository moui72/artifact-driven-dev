# artifact-driven-dev — Project Status

_Updated: 2026-07-26 (**slate bundle planned + tasked on `feat/complexity-model-routing` (draft PR #29)** — `/ardd-plan --slate` over the 3 backlogged items recommended one bundle (`plan-time-complexity-stamps` → `delegate-model-routing`: dependency edge — the routing map is keyed by the complexity stamp — plus shared validator files; both high confidence) and one solo-deferral (`ci-status-refresh-on-main-coll`: low — no code seam, collides with the single-writer rule and the STATUS.md-assembly mechanization non-goal; vet via `/ardd-research` before planning). The bundle run drafted and approved `plan-feat-complexity-model-routing-2026-07-25-fd96.md` (no artifact changes — workflow-field precedent; Open-Question defaults accepted: `haiku|sonnet|opus` tier aliases, absent complexity never routes down) and generated `tasks-feat-complexity-model-routing-884c.md` (`ready`, 0/9, phases: complexity stamp machinery → plan-side wiring → delegate_model grammar → dispatch resolution + docs → verification). Both features flipped `backlogged → tasked`; state committed and pushed, in-flight via draft PR #29. Work Queue: the one `ready` file, no pairs to relate. Register: 1 backlogged · 0 planned · 2 tasked · 44 implemented · 1 retired. 0 open feedback; 0 defects (last verified 2026-07-12). Recommended next step: `/ardd-implement` to execute the tasks file on this branch.)_

_Updated: 2026-07-25 (**PRs #24/#26 merged + reinstall at `541b283` + first live STATUS.md prune** — both feature PRs squash-merged to `main`: #24 (`/ardd-plan --slate` spans features + open feedback, with the review-hardened deterministic bucket precedence/transitive bundles/stable ordering) and #26 (`status_history_keep` keep-last-N STATUS.md pruning, with the review-hardened fail-closed `status-prune.sh`, the shared ≤4-digit validator contract, and the new `ardd-state.sh unstamp` return-to-unbounded path); the `status-md-trim-prune-history` register flip rode #26 (`implemented`, tasks `completed` 15/15). `/ardd-update` reinstalled from this self-hosted checkout at `541b283` (0d60d69 → 541b283; no migrations pending; `status-prune.sh` + `unstamp` now installed), and its backfill ask stamped `status_history_keep: 5` — this very entry's write is the field's first dogfood: the prune below cuts the chronology to its newest 5 blocks, with the full history in git (`git log -p .project/STATUS.md`). Current state on `main`: constitution stable ✅; 0 open defects (last verified 2026-07-12); 0 open feedback; register 3 backlogged · 0 planned · 0 tasked · 42 implemented · 1 retired; no ready/in-progress tasks; no worktrees; no orphaned flips. Recommended next step: merge this update PR, then `/ardd-plan --slate` over the 3 backlogged slugs (`ci-status-refresh-on-main-coll`, `delegate-model-routing`, `plan-time-complexity-stamps`).)_

_Updated: 2026-07-24 (**implemented `ardd-plan-slate-mixed-feature` on branch `feat/ardd-plan-slate-mixed`** — `/ardd-implement` executed all 10 tasks of `tasks-ardd-plan-slate-mixed-feature-4469.md` (`status: completed`, 10/10) inline on this branch: `/ardd-plan --slate` now enumerates open feedback files alongside backlogged features (each feedback *file* is one slate item), grades/relates them on the same two axes, adds the `## Reconsidered`-vs-slated-feature dependency-edge heuristic, and emits mixed `/ardd-plan <slug|feedback-*.md>` recommendations — all while staying read-only (slate never marks or flips feedback). Edits: `skills/ardd-plan/SKILL.md` (Usage `--slate` + slate steps 1–5 + next-step), `skills/ardd-feedback/SKILL.md` (Consumption note), `docs/reference/skills/ardd-plan.md` (hand-written body). No scripts or `lint-project.sh` enum changes — the scoped-run grammar already supported mixed calls. `lint-docs.sh` + `lint-project.sh` clean. Feature flipped `tasked → implemented` (rides this branch; lands when its PR merges). Sibling check: `all_complete=true`. This branch is one of two forked from `main` for the two independent features (the other, `feat/status-md-trim-prune`, carries `status-md-trim-prune-history` planning, still `tasked`). Recommended next step: push `feat/ardd-plan-slate-mixed` + open its draft PR (collaborative mode — awaiting user confirmation to push). Prior blocks preserved verbatim below.)_

_Updated: 2026-07-24 (**delegated `/ardd-implement` run — docs-sweep-feedback fixes completed on this branch (PR #19)** — all 6 tasks of `tasks-chore-docs-sweep-feedback-745e.md` are complete (`status: completed`, 6/6): T001-T005 fixed the five stale `docs/reference/skills/*.md` hand-written passages (`ardd-defects` reconciliation spot-check clause, `ardd-implement` Pre-flight four-path coverage + artifacts-dir ask, `ardd-init` overclaim-guard distinction, `ardd-plan` `plan_preview_editor` coverage, `ardd-update` Claude pre-`--harness` fallback note), T006 verified `lint-docs.sh` green. All 5 bound feedback files remain `planned` against `plan-chore-docs-sweep-feedback-2026-07-24-8c58.md`; no feature slugs (`features: []`), so no register flip. State rides this branch (`worktree-agent-a9ab51aacfd6ed53a`) per worktree-native state and lands when PR #19 merges — the `ready` copy of this tasks file on `chore/docs-sweep-feedback` (PR #20) reconciles to `completed` at that point. This entry refreshes the branch's stale pre-implementation STATUS.md at the user's request; the prior blocks below are preserved verbatim.)_

_Updated: 2026-07-24 (**`/ardd-plan` — docs-sweep feedback planned and tasked, then `/ardd-status` refresh** — this session first rebased local `main` onto `origin/main` (2 local-only commits turned out already applied upstream, skipped as duplicates by `git pull --rebase`), then pruned 8 stale local branches/worktrees whose PRs were confirmed merged via `gh pr list` (`worktree-agent-{a130c3d8,a61ca082,ac39c1f4,ae8b712a}` — PRs #14/#18/#15/#17 — force-removed via `git worktree remove --force` + `git branch -D`; plain branches `chore/feedback-status-readiness-review`, `chore/status-and-devx-feedback`, `collaborative-mode-setup`, `fix/release-notes-sync` — PRs #12/#16/#9/#11 — force-deleted), each confirmed with the user first since git's own ancestry check couldn't see it (squash-merged, not fast-forward). Left `docs/site-improvements` untouched — its PR #3 was closed, not merged, and it carries real unmerged content (mkdocs nav/troubleshooting work) with nowhere else recorded. A branch mix-up was caught along the way: `chore/status-and-devx-feedback` (stashed uncommitted docs-sweep feedback work) turned out to already be fully merged (PR #16) under a different plan's scope — moved the stashed content to a fresh `chore/docs-sweep-feedback` branch off updated `main` instead. Committed 5 new `/docs-sweep`-sourced feedback files there (all `F001` bugs/UX against stale `docs/reference/skills/*.md` pages — `ardd-defects`'s missing reconciliation-spot-check clause, `ardd-implement`'s stale Pre-flight bullet, `ardd-init`'s missing overclaim-guard distinction, `ardd-plan`'s missing `plan_preview_editor` coverage, `ardd-update`'s missing Codex-vs-Claude pre-`--harness` behavior split). A bare `/ardd-plan` (branch check: `on_default=false`, slug derived from current branch) found no unsurfaced defects and no other open feedback — all 5 new files were the only plannable input, all accepted (no `## Reconsidered` items, no negotiation needed). No artifact changes (only `constitution.md` exists; none of the 5 fixes touch a declared principle). Drafted, approved, and tasked `plan-chore-docs-sweep-feedback-2026-07-24-8c58.md` — 1 phase/6 tasks in `tasks-chore-docs-sweep-feedback-745e.md` (`ready`): T001-T005 each fix one reference page's stale passage (parallel, disjoint files), T006 runs `lint-docs.sh` to verify. No feature slugs (`features: []`). All 5 feedback files now `planned`, bound to this plan. Open feedback now 0. Backlog unchanged: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired. No orphaned completion flips found (no `completed` tasks files exist). `lint-project` clean. `workflow_mode: collaborative` — nothing pushed yet; the plan, tasks file, and feedback-file flips are still on disk, uncommitted, on `chore/docs-sweep-feedback`. Per `next_step_prompt: auto`, auto-running `/ardd-implement` next.) Prior update, 2026-07-24 (**PR #17 — delegation-alignment fix implemented and STATUS.md legacy tail removed** — `feedback-delegation-worktree-align-collab-branch-76c4.md`'s fix has shipped on this branch: `skills/ardd-implement/SKILL.md` step 3's collaborative-mode delegation preamble now passes `branch-info.sh`'s `current` ref (or `default` when already `on_default`) explicitly to `worktree-align.sh <ref>`, replacing the fold-to-main requirement, making the push+draft-PR offer visibility-only, and documenting the `aligned=false` stop-and-surface rule, the cross-branch-visibility caveat, and the redundant-draft-PR note. `feedback-status-md-legacy-tail-section-87b6.md`'s stale legacy `## Recent Releases`/`## Feature Backlog` tail is also removed from this very file as part of the same PR. Both feedback files are `status: planned`, bound to `plan-chore-status-and-devx-feedback-2026-07-23-7fb8.md`, `tasks-chore-status-and-devx-feedback-f3c8.md` (3/3 complete). Superseding the prior entry's "not yet implemented" wording below, which described F76c4 only as filed, not yet fixed. Pushed as `worktree-agent-ae8b712a6172862fd`, draft PR #17 — awaiting review/merge; not yet reaped.) Prior update, 2026-07-23 (**`/ardd-implement` coordinator — both chore-feedback-status-readines tasks files delegated, implemented, and opened as draft PRs (collaborative mode, no local merge)** — a bare `/ardd-implement` found 2 independent `ready` tasks files (`parallel-matrix.sh`: no shared features/artifacts) and both were selected, triggering a fan-out. Pre-flight surfaced 4 uncommitted plan/tasks files plus `STATUS.md` and 4 feedback files; user confirmed committing everything on the current branch (`chore/feedback-status-readiness-review`), then — since `worktree-align.sh` only ff's *local* `main` and the work was on a feature branch — ran `fold-to-main.sh` (local ref forward only, no push) to make it visible to the delegated worktrees, pushed the branch, and opened draft PR #13 as the plan/tasks record. Delegated both tasks files to parallel `isolation: "worktree"` subagents. **`tasks-chore-feedback-status-readines-6289.md`** (badge-sync + test isolation): all 6 tasks completed — `ardd-badge.yml`'s "Commit if changed" step now opens a PR via `gh pr create` (+ best-effort `gh pr merge --auto --squash`) instead of pushing to `main`; `test-ardd-update-check.sh`'s producer-contract section now runs `install.sh` against an untagged clone of this repo's own HEAD, decoupling the "Channel: stable by default" assertion from live tag state — 42/42 assertions pass. Pushed as `chore/badge-sync-pr-rework`, opened draft PR #14. **`tasks-chore-feedback-status-readines-5920.md`** (stamp arg-safety + pre-flight gap): all 6 tasks completed — `cmd_stamp` now rejects unexpected trailing arguments via `dieu` (fixed in both `scripts/ardd-state.sh` and the installed copy); `/ardd-implement`'s delegation pre-flight now resolves and covers bound feature-register files, bound feedback files, and `.project/artifacts/` alongside the plan/tasks pair — 137/137 `test-ardd-state.sh` cases pass, `lint-docs`/`lint-project` clean. Pushed as `chore/stamp-arg-safety-and-preflight-gap`, opened draft PR #15. Both worktrees' `core.bare`/`core.hooksPath` were unaffected — no repair needed. **No local merge performed**: `workflow_mode: collaborative` merges through PR review, not `merge_policy` — both worktrees stay unreaped (in flight) until their PRs merge; `worktree-reap.sh --dry-run` reports no candidates yet. **Mid-session tangent, now filed as feedback**: the user flagged the pre-flight-commit → fold-to-main → push → PR sequence above as bad collaborative-mode DevX and asked for a better mechanism; a `fable`-model design consult confirmed `worktree-align.sh` already accepts an optional `<ref>` argument (not main-specific) and recommended aligning a delegated worktree against the *current feature branch* instead of local `main` — transports everything the plan run touched (closing the artifact-gap class of bug generally, not just the two files `44dc`'s fix covers) with shared commit ancestry across parallel worktrees, no local-`main`-touching fold step, and push/PR becomes visibility-only rather than a delegation prerequisite. Filed as `feedback-delegation-worktree-align-collab-branch-76c4.md` (`open`, F001), not yet implemented — the next `/ardd-plan` will pick it up. Open feedback: 1 (this file). Backlog unchanged: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired. `lint-project` clean.) Prior update, 2026-07-23 (**`/ardd-plan` — ardd-state.sh stamp arg-safety + delegation pre-flight artifact-gap fixes planned and tasked, then `/ardd-status` refresh** — a second bare `/ardd-plan` this session found the feature backlog still empty and no unsurfaced defects, offered the 2 feedback files filed mid-session via the step-1a picker, and both were selected: `feedback-ardd-state-stamp-silent-extra-args-1625.md` (`ardd-state.sh stamp` silently drops trailing positional args past its first `file key val` triple) and `feedback-delegation-preflight-artifact-gap-44dc.md` (`/ardd-implement`'s delegation pre-flight only covers the plan+tasks pair, missing artifact/feature/feedback edits from the same plan run). No artifact changes needed. Drafted, approved, and tasked `plan-chore-feedback-status-readines-2026-07-23-a4a4.md` — 6 tasks/2 phases in `tasks-chore-feedback-status-readines-5920.md` (`ready`): Phase 1 adds a trailing-argument guard to `cmd_stamp` (`ardd-state.sh:330-332`) — `shift 3` then `dieu` if anything remains — with red-first/green regression cases in `scripts/test-ardd-state.sh`; Phase 2 widens `/ardd-implement`'s pre-flight (`ardd-implement/SKILL.md:161-195`) to also resolve the plan's `features:` list to `.project/features/<slug>.md`, feedback files whose `plan:` frontmatter names this plan, and `.project/artifacts/` as a whole directory — folding all three into the existing `git status --short`/`git add` coverage alongside the plan/tasks pair, verified by manual trace (no test harness exists for this prose-only skill behavior). Both feedback files' F001 items marked `[x]` and flipped to `planned`, bound to this plan. Two `ready` tasks files now coexist on this branch (`tasks-chore-feedback-status-readines-5920.md` and the earlier `tasks-chore-feedback-status-readines-6289.md` from this same session's first `/ardd-plan` run) — `parallel-matrix.sh` reports them `independent` (no shared features, no shared `[artifacts: ...]` tags), safe to implement in either order or fan out. `workflow_mode: collaborative` — nothing pushed or PR'd this session; **uncommitted on disk**: `STATUS.md`, 4 feedback files (2 newly-marked from this run, 2 from the prior run), 2 plans, and 2 tasks files — all still the user's to commit/push. Open feedback now 0. Backlog unchanged: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired. No orphaned completion flips found. `lint-project` clean.) Prior update, 2026-07-23 (**`/ardd-plan` — badge-sync branch protection + test-isolation fixes planned and tasked, then `/ardd-status` refresh** — a bare `/ardd-plan` found an empty feature backlog and no unsurfaced defects, offered the 2 open feedback files via the step-1a picker, and both were selected: `feedback-branch-protection-badge-exception-a8a9.md` (badge-sync workflow can't push to `main` under the new branch-protection rule) and `feedback-test-ardd-update-check-channel-0bc9.md` (the "Channel: stable by default" test assertion is order/timing-dependent on this repo's own live tags). No artifact changes needed (no feature slugs targeted; only `constitution.md` exists and neither fix touches a declared principle). Drafted, approved, and tasked `plan-chore-feedback-status-readines-2026-07-23-659e.md` — 6 tasks/2 phases in `tasks-chore-feedback-status-readines-6289.md` (`ready`): Phase 1 reworks `.github/workflows/ardd-badge.yml`'s "Commit if changed" step to open a PR against `main` (short-lived branch + `gh pr create`, with a best-effort `gh pr merge --auto` that never hard-fails the workflow if it can't merge itself) instead of pushing directly; Phase 2 has `scripts/test-ardd-update-check.sh`'s producer-contract section run `install.sh` against a fresh untagged clone of this repo's own `HEAD` (via `git init` + `git fetch --depth 1 <repo> HEAD` + checkout `FETCH_HEAD`) rather than the live checkout directly, so `install.sh`'s tag-inference fallback can never see a beta tag `beta-release.yml` might have landed on `HEAD` in the meantime. Both feedback files' F001 items marked `[x]` and flipped to `planned`, bound to this plan. Two more feedback items were filed mid-run (not part of this plan, left `open` for the next `/ardd-plan`): `feedback-ardd-state-stamp-silent-extra-args-1625.md` (`ardd-state.sh stamp` silently drops extra positional args past the first `file key val` triple instead of erroring or supporting multi-pair stamping) and `feedback-delegation-preflight-artifact-gap-44dc.md` (`/ardd-implement`'s delegation pre-flight only commits the plan+tasks pair, missing artifact/feature-register/feedback edits the same plan run may have produced — so those edits stay invisible to a delegated worktree). `workflow_mode: collaborative` (constitution frontmatter) — this session did not push or open a branch/PR; **uncommitted on disk**: the 2 newly-filed feedback files, the 2 marked/flipped existing feedback files, the new plan, and the new tasks file — all still the user's to commit and push per collaborative mode's PR-based flow. Open feedback now 2 files (both new, both `open`). Backlog unchanged: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired. No orphaned completion flips found. `lint-project` clean.) Prior update, 2026-07-23 (**`/ardd-feedback` — logged the branch-protection badge-sync exception request** — wrote `feedback-branch-protection-badge-exception-a8a9.md` (`open`, 1 bug, F001): the "ArDD version badge sync" workflow can no longer push its refresh commit directly to `main` since commit `0a4d52a` added branch protection requiring PR review — needs either a bypass allowance for the workflow's actor on the `main` protection ruleset, or a rework to PR-and-merge instead of pushing directly. Not release-blocking (only the version badge goes stale), left untagged (no matching artifact — this is a repo-settings/workflow fix). Open feedback now 2 files (this one plus `feedback-test-ardd-update-check-channel-0bc9.md` from earlier this session); the other 2 files on disk remain `status: planned`, not open. Backlog unchanged: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired. `lint-project` clean. No feature/tasks state touched.) Prior update, 2026-07-23 (**`/ardd-feedback` — logged a test-timing-artifact bug found during stable-release readiness review** — wrote `feedback-test-ardd-update-check-channel-0bc9.md` (`open`, 1 bug, F001): `scripts/test-ardd-update-check.sh`'s "install.sh records Channel: stable by default" assertion (~line 238) is silently order/timing-dependent, not a real channel-inference bug — it runs `install.sh` against this repo's own checkout and expects the SOURCE_REF fallback to land on `stable`, but `beta-release.yml` tags every push to `main`, so the assumption only holds in the brief window before that tag lands; `lint.yml` checks out before `beta-release.yml` runs so CI stays green, but a local re-run after tags catch up fails deterministically (observed: `beta` instead of `stable`). Not a functional `install.sh` bug — real installs go through `new.sh`, which always sets `$ARDD_CHANNEL` explicitly and never hits this fallback. Left untagged (no `infrastructure.md` or other matching artifact exists — `lint-project` flagged an initial `[artifacts: infrastructure]` tag as invalid, removed it). Found alongside a broader readiness check this session: repo is clean to cut stable — CI green on `main` tip, `lint-docs`/`lint-project` clean, 46/47 regression tests pass (this being the one failure, non-blocking), and the Codex/multi-harness scenario-sweep coverage question was resolved by discovering `dev-notes/scenario-runs/2026-07-22-c0de/` already ran a full Codex-side sweep against that exact feature with fixes already merged — no new sweep needed. Also separately noted (not this session's to fix): the "ArDD version badge sync" workflow now fails pushing directly to `main` since branch protection was added — cosmetic, and `stable-release.yml`'s own pushes land on `release`, which has no PR-review rule, so the release mechanics are unaffected. Open feedback now 1 file (2 others on disk — `feedback-amend-path-for-skill-written-files-2187.md`, `feedback-repo-critique-6ad1.md` — are both already `status: planned`, not open). Backlog unchanged: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired. No feature/tasks state touched.) Prior update, 2026-07-22 (**`/ardd-implement` coordinator — codex-scenario-sweep-fixes implemented and merged** — the delegated `worktree-agent-aa8fe9537adca84ce` run completed both tasks and reported back; merge was a clean fast-forward (`002e162..456a87b`), `core.bare`/`core.hooksPath` unaffected, worktree reaped — no stray-commit recovery needed this time (the subagent was warned about the prior run's `cd` mistake and stayed correctly scoped to its worktree throughout). Shipped: `skills/ardd-defects/SKILL.md` step 5 gained the reconciliation sub-step (spot-check any claim present in the prior `DEFECTS.md` but missing from a fresh survey before letting it drop — closes F002); `skills/ardd-status/SKILL.md` step 6 gained the explicit prepend-and-preserve rule this very entry follows (closes F003) — codifying into skill prose the convention this `STATUS.md` file has followed all session. Full 47-script regression suite re-verified green by this coordinator post-merge. No bound features (`features: []`). Open feedback: 0. Backlog fully quiescent: 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired.) Prior update, 2026-07-22 (**`/ardd-plan feedback-codex-scenario-sweep-findings-565e.md` — approved and tasked** — F002 and F003 accepted; F001 investigated and declined as stale (the current `skills/ardd-update/SKILL.md` text already reads "Claude-oriented `.claude/skills` output" — no trace anywhere in the repo of the reported wrong `.agents/skills` wording; likely already fixed by the time this item was re-filed, or based on a stale observation). Drafted, approved, and tasked `plan-codex-scenario-sweep-fixes-2026-07-22-220d.md` — 2 phases/2 tasks in `tasks-codex-scenario-sweep-fixes-427b.md` (`ready`): T001 adds a reconciliation sub-step to `/ardd-defects` step 5 — before its full-overwrite regeneration, diff the fresh survey against the current on-disk `DEFECTS.md` and spot-check any claim present before but missing now, rather than trusting general survey completeness implicitly (closes F002's silent-drop gap while keeping the "no manual removal needed" design intact); T002 adds an explicit prepend-and-preserve rule to `/ardd-status` step 6 — a new `_Updated:` entry prepends, every prior entry stays verbatim below it, never summarized away (codifies into skill prose the convention this very `STATUS.md` file has followed all session, closing F003's gap for any future agent/session that might not). Both are prose-only fixes with no test requirement. No feature slugs (`features: []`). Feedback file now `planned`. Open feedback now 0. `lint-project` clean.) Prior update, 2026-07-22 (**`/ardd-feedback` — re-filed the 3 orphaned codex-scenario-sweep items** — the prior `feedback-codex-scenario-sweep-findings-2eee.md` had all 3 of its items declined into an unrelated plan (`plan-plan-preview-editor-option-2026-07-22-3276.md`) earlier this session, leaving them untracked by any plan. User asked to re-log them: wrote `feedback-codex-scenario-sweep-findings-565e.md` (`open`, 2 bugs + 1 UX, verbatim content, fresh F001–F003 IDs) — F001 `$ardd-update`'s Codex pre-harness guard names `.agents/skills` in wording that should be harness-neutral; F002 `$ardd-defects` drops still-open known defects when regenerating `DEFECTS.md`, under-reporting `STATUS.md`; F003 `$ardd-status` compresses lived-in `STATUS.md` too aggressively (a real, still-unaddressed instance of exactly the kind of over-compression risk this same STATUS.md file keeps carrying entry-by-entry). Open feedback now 1 file. `lint-project` clean. Prior update, 2026-07-22 (**`/ardd-implement` coordinator — codex-second-harness-support closeout implemented and merged, with a mid-run recovery** — the delegated `worktree-agent-a6f6fb20baaba1bcd` run completed all 4 tasks (`templates/AGENTS.md` pointer file; `install.sh`'s never-clobber `AGENTS.md` write/advisory block, gated on `--harness codex`, mirroring the `.github/badges/ardd-icon.svg` idiom; `scripts/test-install-agents-md.sh`, 5/5 passing; `docs/install.md` mention; plus an out-of-plan but correctly-scoped fix — wired the new test script into `.github/workflows/lint.yml`'s explicit per-script job list, having discovered CI uses an explicit list, not a glob) and reported back. **Recovery needed**: the subagent's very first `ardd-state.sh tasks-flip ... in-progress` command was accidentally run against the primary checkout instead of its worktree (a `cd` mistake — Bash has no isolation guard the way the Write tool does), landing a stray commit directly on local `main` that also incidentally swept in the earlier uncommitted `/ardd-update` output (badges, workflow, README, `ardd-version.md` — legitimate content, just bundled oddly). This made `main` one commit ahead of the worktree branch's merge-base, so the merge (`merge_policy: auto`) hit a real conflict — confined to the tasks file's status/checkbox lines (`main`: stale `in-progress`/unchecked; worktree: correct `completed`/all-checked) — and per protocol (never auto-resolve a surfaced conflict) this coordinator aborted the merge and asked the user, who confirmed taking the worktree's tasks-file version. Merged with `-X theirs` scoped to that one file (`git merge --no-ff -X theirs`); the rest of the stray commit's content (badges/README/etc.) merged in cleanly alongside it, unaffected. Full 47-script regression suite re-verified green by this coordinator post-merge (not just trusting the subagent's report); `core.bare`/`core.hooksPath` unaffected; worktree reaped. Feature `codex-second-harness-support`: `tasked` → `implemented` — the feature is now fully closed out (constitution's Multi-harness install section, install.sh --harness codex, new.sh --harness codex, the $ardd-* sigil rewrite, .worktreeinclude handling, a live Codex smoke test, and now AGENTS.md guidance, all shipped). Open feedback: 0. Backlog now 0 backlogged · 0 planned · 0 tasked · 40 implemented · 1 retired — fully quiescent.)

